.block1
  .head
    %h2.title Municipes
  .content
    = simple_table_for @people, :name, :sex_text, :marital_text, :profession_state_text
    = will_paginate @people

- sidebar do
  = link "Novo Registro", new_person_path, :class => 'icon iadd'
  = link "Relatorio", "/"
