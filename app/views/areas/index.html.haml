.block
  .content
    %h2.title Areas
    .inner
      = simple_table_for @areas, :name
      = will_paginate @areas

- content_for :sidebar do
  .block
    %h3 Menu
    %ul.navigation
      %li= link_to image_tag('../images/icons/add.png', :class=>'img')+' Novo Registro', new_area_path
