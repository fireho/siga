.block
  .content
    %h2.title Propriedades
    .inner
      = simple_table_for @properties, :name
      = will_paginate @properties

- sidebar do
  = link "Novo Registro", new_property_path, :class => 'icon iadd'

